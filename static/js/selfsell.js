/**
 * Created by chenhao on 2016/5/27.
 */
var app = require('mslApp');
app.controller('SelfSellCtrl', ['$scope', '$http', '$rootScope', 'ScrollFixed', function ($scope, $http, $rootScope, ScrollFixed) {
    $rootScope.pageName = 'box selfsell-box';
    ScrollFixed(false);
    //checkbox
    var typeList = [{

    }];

}]);
